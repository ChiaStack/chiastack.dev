---
title: Getting Started
description: Get started with Chia Stack
---

This guide will help you quickly get started with Chia Stack packages.

## Installation

Each package in Chia Stack can be installed and used independently.

### Utils

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/utils
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/utils
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/utils
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/utils
    ```
  </Tab>
</Tabs>

### Services

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/services
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/services
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/services
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/services
    ```
  </Tab>
</Tabs>

### UI

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @chiastack/ui
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @chiastack/ui
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @chiastack/ui
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @chiastack/ui
    ```
  </Tab>
</Tabs>

## Next Steps

- Check out the [Utils documentation](/docs/utils) to learn about all available utility functions
- Check out the [Services documentation](/docs/services) to learn about available service integrations
- Check out the [UI documentation](/docs/ui) to learn about available components

